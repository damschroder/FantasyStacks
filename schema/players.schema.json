{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fantasystacks.dev/schema/players.schema.json",
  "title": "FantasyStacks players",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "data"],
  "properties": {
    "schemaVersion": { "const": "1.3.0" },
    "data": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["playerId", "name", "position", "latestTeam", "headshotUrl", "ecr", "ecrUpdatedAt", "sourceIds"],
        "properties": {
          "playerId": { "type": "string", "minLength": 1 },
          "name": { "type": "string", "minLength": 1 },
          "position": { "enum": ["WR", "TE", "RB", "QB"] },
          "latestTeam": { "type": ["string", "null"] },
          "headshotUrl": { "type": ["string", "null"] },
          "ecr": { "type": ["number", "null"], "exclusiveMinimum": 0 },
          "ecrUpdatedAt": { "type": ["string", "null"], "format": "date" },
          "sourceIds": {
            "type": "object",
            "additionalProperties": false,
            "required": ["gsis", "pfr"],
            "properties": { "gsis": { "type": "string" }, "pfr": { "type": ["string", "null"] } }
          }
        }
      }
    }
  }
}
